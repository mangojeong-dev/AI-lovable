services:
  - type: web
    name: vue-workout-lovable-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run api
    envVars:
      - key: NODE_ENV
        value: production
